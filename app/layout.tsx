import './globals.css'
export const metadata={title:'BREAKTHROUGH — Stories that were never filmed.',description:'A new kind of streaming service for original cinematic stories.'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
