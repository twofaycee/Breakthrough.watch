'use client'
import {useState,useEffect} from 'react'
import {FILMS,GENRES} from '../lib/films'
export default function Page(){const [films,setFilms]=useState(FILMS);const [genre,setGenre]=useState('All');useEffect(()=>{fetch('/api/films').then(r=>r.json()).then(d=>{if(Array.isArray(d)&&d.length>0) setFilms(d)})},[]);const f=genre==='All'?films:films.filter(x=>x.genre===genre);return <main style={{background:'#050505',color:'#fff',minHeight:'100vh',padding:20}}><div style={{display:'flex',justifyContent:'space-between'}}><div style={{fontWeight:900}}>BREAKTHROUGH.AI - ULTRA ROBUST BUILD</div><a href='/bot' style={{background:'#22c55e',color:'#000',padding:'8px 14px',borderRadius:999,textDecoration:'none',fontWeight:900}}>Bot</a></div><h1 style={{fontSize:48,fontWeight:900,marginTop:20}}>Every film<br/>was never<br/><span style={{color:'#E50914'}}>filmed.</span></h1><div style={{display:'flex',gap:8,marginTop:12}}>{GENRES.map(g=><button key={g} onClick={()=>setGenre(g)} style={{background:genre===g?'#fff':'#222',color:genre===g?'#000':'#fff',border:'none',padding:'6px 12px',borderRadius:999}}>{g}</button>)}</div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(200px,1fr))',gap:12,marginTop:20}}>{f.map(x=><div key={x.id} style={{background:'#111',borderRadius:12,overflow:'hidden'}}><video src={x.video_url} muted style={{width:'100%',height:200,objectFit:'cover'}}/><div style={{padding:10}}>{x.title}</div></div>)}</div></main>}
